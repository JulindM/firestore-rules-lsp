service cloud.firestore {
  match /path {
    allow read;

    match /path/one {
      allow write: if var;
    }

    match /{path}/one {
      allow create;
    }

    match /{path}/{wild=**} {
      allow read;
    }
  }

  match /path {
    allow get;
  }
}